let data,round=1;const $=s=>document.querySelector(s);const names=['Alpha','Bravo','Charlie','Delta','Echo','Foxtrot','Golf','Hotel','India','Juliett'];function table(){return names.map((name,i)=>({name,i,p:0,w:0,d:0,l:0,gf:0,ga:0,gd:0,pts:0})).sort((a,b)=>a.i-b.i)}function render(){const rows=table();$('#standings').innerHTML=rows.map((r,i)=>`<tr class="pair${Math.floor(i/2)}"><td>${i+1}</td><td>${r.name}</td><td>${r.p}</td><td>${r.w}</td><td>${r.d}</td><td>${r.l}</td><td>${r.gf}–${r.ga}</td><td>${r.gd}</td><td>${r.pts}</td></tr>`).join('');const ms=data.matches.filter(m=>m.round===round);$('#fixtures').innerHTML=ms.map(m=>`<article class="fixture"><span>${m.start}–${m.end}<br>Bane ${m.pitch}</span><strong>${m.home||`${m.ranks[0]}. plass`}</strong><strong>${m.away||`${m.ranks[1]}. plass`}</strong></article>`).join('');document.querySelectorAll('.round').forEach(b=>b.classList.toggle('active',+b.dataset.round===round))}fetch('tournament.json').then(r=>r.json()).then(v=>{data=v;$('#rounds').innerHTML=Array.from({length:10},(_,i)=>`<button class="round" data-round="${i+1}">${i===9?'Sluttspill':'Runde '+(i+1)}</button>`).join('');$('#rounds').onclick=e=>{if(e.target.dataset.round){round=+e.target.dataset.round;render()}};render()});document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{document.querySelectorAll('[data-view]').forEach(x=>x.classList.toggle('active',x===b));document.querySelectorAll('main section').forEach(x=>x.hidden=x.id!==b.dataset.view)});
